# 秋填 · 个人网申助手

这是根据参考视频中的操作流程实现的个人版浏览器扩展，适用于 Chrome / Edge。版本 0.1.2。代码和资料存储都在本地，基础功能不需要账户、服务器或 AI Key。

## 安装

1. 解压 `秋填-Chrome-Edge.zip`，保留解压后的文件夹。
2. Chrome 在地址栏输入 `chrome://extensions`；Edge 输入 `edge://extensions`。
3. 打开页面中的「开发者模式」。
4. 点击「加载已解压的扩展程序」。
5. 选择解压后的 `autumn-fill/extension` 文件夹。这一层直接包含 `manifest.json`，不要选择整个交付包的外层目录。
6. 在浏览器工具栏的扩展菜单中固定「秋填 · 个人网申助手」。
7. 更新代码后，在扩展管理页点击秋填的刷新按钮，再刷新招聘网页并重新打开助手。

代码无需 npm 安装或编译。两个浏览器各自保存资料，不会自动互相同步。

## 第一次使用

1. 点击秋填图标，再点「管理我的简历」。
2. 在资料库中填写个人信息和各段经历。左侧分类可以随时切换；修改后点「保存资料」。
3. 教育、工作、实习、项目等多段经历分别点击「添加一条」。资料顺序应与招聘网页对应经历的顺序一致。
4. 打开招聘网站并登录，进入真正的简历填写页面。
5. 点击浏览器工具栏秋填图标，再点「在当前页面打开助手」。
6. 可以先「扫描页面」，查看识别到的字段，再「一键填写」。
7. 检查页面填写结果、日期和下拉选项。最终提交由你在招聘网站手动完成。

扩展不默认装载示例内容。交付包中的 `示例简历.json` 全部是虚构测试资料，只用于本地试用，不要用于真实投递。

## 视频中的功能对应

| 视频行为 | 本版操作与含义 |
| --- | --- |
| 开始填写 | 一键填写：识别当前页面可编辑的空白字段，匹配当前简历并逐项填写。 |
| 进度与停止 | 显示本轮进度、已填写数量、待处理数量；可以停止后继续。 |
| 继续填写 | 重新扫描页面中新增或仍为空白的字段；已有内容保留。 |
| 辅助填写 | 点击面板里的资料标签，再点击网页输入框，把选中的资料填入目标位置。 |
| 搜索字段 | 按字段名、别名或资料内容搜索；按类别筛选。 |
| 拖动面板 | 按住面板顶部拖动。顶部「−」折叠；「×」关闭，再次点击扩展图标可以重新打开。 |
| 编辑简历 | 从面板进入资料管理，可修改、添加经历并保存。 |
| 切换简历 | 资料管理顶部切换当前简历；可以新建、命名不同岗位版本。切换后回到网页重新扫描。 |

新增的辅助能力包括逐字段结果、定位输入框、复制资料内容、撤销本次填写、JSON 导入导出，以及可选 AI 识别。

## 辅助填写与撤销

- **点选辅助**：先点资料标签，然后点网页上需要填写的输入框。输入框已有内容时，会询问是否替换。
- **取消选择**：按 Esc。
- **复制所选内容**：选中资料后点击复制，适合某些不接受脚本赋值的控件；可手动粘贴。
- **面板遮挡**：拖动面板顶部，或临时折叠，让目标输入框露出来。
- **撤销本次填写**：恢复本轮一键填写以及之后辅助填写所改动的字段。只有当前值仍与插件写入值一致时才撤销；人工随后修改过的内容保留。重新启动一轮一键填写会建立新的撤销记录。关闭标签页、刷新页面后，撤销记录不保留。部分自定义单选不允许恢复到未选择状态，这类项目会提示手动恢复。

## 资料类别

1. 个人信息：姓名、英文名、性别、手机号码、邮箱、出生日期、国籍、民族、政治面貌、现居城市、籍贯、联系地址、证件号码、个人主页、自我评价。
2. 求职意向：意向岗位、期望工作地点、期望薪资、预计入职时间。
3. 教育经历：学校、专业、学历、学位、开始和结束时间、GPA、专业排名、主修课程、描述。
4. 工作经历：公司、职位、部门、开始和结束时间、工作描述。
5. 实习经历：公司、实习岗位、开始和结束时间、实习描述。
6. 项目经历：名称、角色、开始和结束时间、链接、项目描述、项目职责。
7. 技能特长：技能名称、熟练程度、技能描述。
8. 外语能力：语种、语言等级、考试成绩。
9. 获奖经历：名称、时间、级别、描述。
10. 在校经历：组织名称、担任职务、开始和结束时间、经历描述。
11. 家庭关系：成员姓名、关系、工作单位、职务、联系电话。
12. 专利信息：名称、专利号、申请日期、描述。
13. 论文发表：名称、期刊、发表日期、描述。
14. 专业资格：具有资格证书、证书名称、获得时间。
15. 其他信息：爱好及特长、优势与不足、自我评价及求职目标、其他。

根据中国人寿实际表单，另补充了以下字段，保留在对应类别中：个人信息中的身高（CM）、体重（公斤）、加入党派时间、婚姻状况、高考时户口所在地、紧急联系人及联系方式、健康状况、期望工作城市、期望待遇（万元/年）、是否服从调剂、是否愿意去县级公司工作、是否获得过奖学金、是否为学生干部；教育经历中的第二专业、是否全日制、是否最高全日制学历、是否主修；工作和实习经历中的用工形式、所在部门；获奖经历中的颁奖单位和担当角色；论文中的担任角色；项目中的项目级别；家庭成员中的出生日期。

这些字段只使用你明确保存的值。特别是期望待遇（万元/年）与期望薪资分开录入，不自动把月薪当作年薪。

一个资料版本最多保留每个重复类别 50 段经历，最多 20 个简历版本。导出备份只包含当前简历；如要备份多个版本，请分别切换并导出。

## 导入资料

**手动录入**：不需要 AI，适合第一次整理标准资料。

**JSON 备份**：进入「导入简历」，选择秋填导出的 JSON 文件。检查预览，点击「应用预览，前往检查」，再保存。导入替换的是当前简历，不会合并到已有经历。

**从正文提取**：先配置 AI，把简历正文粘贴到「导入简历」，点击「用 AI 提取资料」。检查 JSON 预览后应用、逐类核对并保存。当前版本不直接解析 PDF、Word 或扫描图片；可先从文件中复制文字。没有提供的事实不应由模型补写。

## 可选 AI

基础自动填写依赖本地字段匹配；AI 提供两个附加功能：

- **简历提取**：把完整正文整理为资料库结构。
- **未匹配字段建议**：在「填写结果」中点击「用 AI 识别未匹配字段」，生成字段与现有资料条目的对应建议，核对后逐条确认填写。

在「AI 接口设置」填写兼容 Chat Completions 的完整接口地址、模型 ID 和 API Key。例如地址应以服务商实际提供的 `/chat/completions` 路径为准，不要只填网站主页。点击「保存并授权接口」后，浏览器会申请访问该接口所属网站的权限。

接口使用 HTTPS；本机 `localhost` / `127.0.0.1` 可以使用 HTTP。请求格式为 `model`、`temperature`、`messages`，响应内容读取 `choices[0].message.content`。提供商需要支持此格式。

只有点击 AI 操作才会发起请求。正文提取发送你粘贴的文本；字段匹配发送网页字段标签、资料条目名称、分类和序号，不发送资料字段值。Key 保存在扩展本地存储中，由后台请求使用，不写入网页，也不包含在导出备份中。API 调用费用取决于你配置的服务商。

## 招聘页面兼容范围

目前支持标准文本输入、文本域、原生下拉框、原生日期/月选择框和可编辑文本区域，能触发 input/change 事件。会读取 label、aria-label、placeholder、name、id 以及附近的标题，结合经历分类和出现顺序匹配字段。

- 页面同源 iframe、开放 Shadow DOM 会参与扫描；跨域 iframe 不在当前版本自动填写范围内。
- 自定义省市联动、树形专业选择、带搜索的学校下拉、只读日期选择器等，可能需要在招聘网页中手动选择；辅助复制可以减少重复输入。
- 不会自动增加经历卡片。先在网站点击「添加」，再点「继续填写空白项」。新增表单片段出现后需要重新扫描。
- 只知道年月时不会自动补出某一天。月资料可填入月份框，日期框需完整日期。
- 无法唯一判断的字段留空，并在结果中标记。多段经历的页面排序如果与资料库不同，请先调整资料或用辅助填写明确选择。
- 密码、验证码操作、上传附件、协议勾选和提交需由你处理。普通验证码输入框没有对应资料，不会由匹配器生成答案。
- 招聘页面刷新、跳转后，再次点击扩展打开助手。

**中国人寿校招 / 北森 zhiye.com**：已检查登录后的实际表单。专用适配器根据 `form-item__text` 读取标签，根据外层章节标题区分本人信息、教育、工作/实习、获奖、语言、项目、家庭成员等，避免把家庭成员姓名当作本人姓名。

自定义普通下拉会点击精确匹配的选项并回读选中状态；民族等弹层会在选项匹配后点击弹层内的「确定」；单选按已有资料选择；日期弹层尝试输入规范日期并确认。找不到唯一选项或无法确认选中状态时，会标记需要人工处理。省市级联、多级树形和特殊控件不保证自动完成。

网站的「工作/实习经历」为合并分组，填写顺序为资料库中的工作经历在前、实习经历在后。先在网站增加所需条目，并核对网页排序。教育经历按资料库顺序填写；该页面提示从最高学历填至高中，应按你的实际情况整理资料库。

本人承诺、签名、违规情况等声明分组不自动匹配，也不提交给 AI 生成答案。真实的同意与提交仍由你操作。

已完成真实页面的结构检查和根据结构构建的本地控件验证；尚未在用户安装后的扩展环境中用真实简历完成整页验收。

## 本地验证与开发

`extension/` 是可加载扩展。`tests/` 是开发测试与浏览器演示，测试桥接仅模拟 Chrome API，不会进入扩展安装目录。

运行不依赖第三方库的测试：

```bash
node tests/engine.test.cjs
node tests/background.test.cjs
```

在交付目录启动本地演示：

```bash
python3 -m http.server 8765 --bind 127.0.0.1
```

浏览器打开 `http://127.0.0.1:8765/tests/demo.html`。点击「运行控件验证」；再打开浮动面板一键填写，点击「核验一键填写结果」。面板遮挡测试按钮时先折叠它。

`tests/options-preview.html` 用于查看和测试资料管理界面。它的存储是测试用 localStorage；实际扩展使用 Chrome storage.local。

使用的扩展机制为 Manifest V3、activeTab、scripting、storage。按需授权当前页后注入脚本，不在安装时要求持续读取所有招聘网页。官方参考：[activeTab](https://developer.chrome.com/docs/extensions/develop/concepts/activeTab)、[chrome.scripting](https://developer.chrome.com/docs/extensions/reference/api/scripting)。


## 完整资料迁移补充

资料库现可保留微信号、QQ号、年龄、证件类型、最高学历、毕业院校与专业、学习形式、毕业时间、英语等级、高考分数与科目、工作年限、紧急联系人关系、应届生/保研/留学/色弱情况、户口性质与所在地、档案所在地、完整个人优势和兴趣爱好。

教育经历增加学号、院系、学校城市、联合培养、学历状态、学制、完整起止时间、学校类型、教育方式、招生类别、挂科数、是否海外学校。实习经历增加起止时间、地区、行业、证明人姓名、联系方式和职位。家庭关系增加性别、教育程度、民族、政治面貌及联系地址。所有字段都可以编辑、备份和辅助填写。

个人资料单独保存在浏览器资料库和用户自己的 JSON 备份中，不写入插件通用安装包。


## 0.1.1 动态表单补填

网页重建普通个人字段后，插件会在原文档中重新寻找同一分类、同一标签和同一资料项的唯一输入框；无法唯一确认时提示重新扫描。若写入过程中控件被换成空白新控件，最多自动补填一次。已有内容保留，多段经历不会按旧序号盲目重新定位。

北森证件号码输入会触发聚焦、输入变化和失焦流程。整轮结束时检查此前填写的内容是否仍在页面上，发现被网站清空时更新结果与计数；撤销对应到已确认的新输入框。整轮结束之后发生的异步服务器校验仍需以网页最终提示为准。


## 智联校园家庭关系（0.1.2）

在智联校园网申页面切换到“家庭关系”，点击“编辑”，显示输入框后再启动秋填。支持“姓名-父亲/母亲”“年龄-父亲/母亲”“工作单位-父亲/母亲”“职位/职务-父亲/母亲”“联系电话-父亲/母亲”“政治面貌-父亲/母亲”等字段。

资料管理的家庭关系分类中，请把每位成员的“与本人关系”明确填写为“父亲”或“母亲”。指定父母的网页字段按关系匹配，资料库排列顺序不影响对应；缺少关系或存在多条相同关系时留空。年龄只填写已保存的年龄，不根据生日自动推算。政治面貌尝试选择当前下拉框唯一对应选项，“共青团员”对应“团员”。没有匹配选项时需手动选择。

“父亲/母亲是否为邮储或邮政体系员工”属于声明，不从工作单位名称推断填写。网页已填写的内容保留。核对完成后请自行点击网站的保存按钮。
